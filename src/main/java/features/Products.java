package features;

public class Products {
    public static void addSingleItem()
    {
        //Tests adding one product from the catalog to the cart and validates the cart badge count.
    }
    public static void addMultipleItems()
    {
        //Verifies adding several products updates the cart count correctly and stores items properly.
    }
    public static void sortProducts()
    {
        //Checks that product sorting (e.g., A–Z, price low–high) works as expected.
    }
    public static void productpage()
    {
        //Validates that clicking a product opens its details page and displays correct information.
    }
}
